<% if current_user.admin? %>

<h1> Les questions sans réponses des utilisateurs</h1>

<% @questions.each do |question|  %>
  <% if question.linked? == false %>
      <p> La question  <p>
      <p><%= question.query %></p>
    <% end %>
<% end %>

   
  <h1> Les questions des utilisateurs</h1>

 <% @questions.each do |question|  %>
  <p> La question  <p>
  <p><%= question.query %></p>
  <p> L'url  <p>
  <p><%= question.link.url %></p>
  <% end %>














<% else %>
  <h1> Index des questions </h1>

  <% @questions.each do |question|  %>
  <p><%= question.query %></p>
  <p>linked? = <%= question.linked %></p>
  <% end %>
<% end %>
